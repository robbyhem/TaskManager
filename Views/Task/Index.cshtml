@model IEnumerable<TaskManager.ViewModels.TaskViewModel>

@{
    ViewData["Title"] = "All Tasks";
}

<div class="container mt-4">
    <h4 class="d-flex justify-content-between align-items-center">
        <span>@ViewBag.HeaderText (@ViewBag.TaskCount)</span>
        <div>
            <a asp-action="Create" class="btn btn-primary">Create Task</a>
            <a asp-action="Index" asp-route-due_date="Due Today" class="btn btn-outline-secondary">Due Today</a>
            <a asp-action="Index" asp-route-due_date="Overdue" class="btn btn-outline-secondary">Overdue</a>
            <a asp-action="Index" asp-route-due_date="No Deadline" class="btn btn-outline-secondary">No Deadline</a>
            <a asp-action="Index" class="btn btn-outline-secondary">All Tasks</a>
        </div>
    </h4>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    @if (Model != null && Model.Any())
    {
        <table class="table table-bordered table-striped mt-3">
            <thead class="table-dark">
                <tr>
                    <th>S/N</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Assigned To</th>
                    <th>Due Date</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int i = 1;
                    foreach (var task in Model)
                    {
                        <tr>
                            <td>@i</td>
                            <td>@task.Title</td>
                            <td>@task.Description</td>
                            <td>@task.AssignedTo</td>
                            <td>@(task.Deadline.ToString("yyyy-MM-dd") ?? "No Deadline")</td>
                            <td>@task.Status</td>
                            <td>
                                
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    }
    else
    {
        <h5 class="mt-4 text-center text-muted">No task found.</h5>
    }
</div>
