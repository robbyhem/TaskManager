@model IEnumerable<TaskManager.ViewModels.TaskViewModel>
@{
    ViewData["Title"] = "My Tasks";
}

<h2 class="mt-4">My Tasks</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Info"] != null)
{
    <div class="alert alert-info">@TempData["Info"]</div>
}

@if (Model != null && Model.Any())
{
    <table class="table table-bordered mt-4">
        <thead>
            <tr>
                <th>S/N</th>
                <th>Title</th>
                <th>Description</th>
                <th>Status</th>
                <th>Priority</th>
                <th>Due Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 1;
                foreach (var task in Model)
                {
                    <tr>
                        <td>@i</td>
                        <td>@task.Title</td>
                        <td>@task.Description</td>
                        <td>
                            @if (task.Status == "Pending")
                            {
                                <span class="badge bg-primary">Pending</span>
                            }
                            else if (task.Status == "In Progress")
                            {
                                <span class="badge bg-warning text-dark">In Progress</span>
                            }
                            else if (task.Status == "Completed")
                            {
                                <span class="badge bg-success">Completed</span>
                            }
                            else if (task.Status == "Overdue")
                            {
                                <span class="badge bg-danger text-dark">Overdue</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">None</span>
                            }
                        </td>
                        <td>
                            @if (task.Priority == "Critical")
                            {
                                <span class="badge bg-black">Critical</span>
                            }
                            else if (task.Priority == "High")
                            {
                                <span class="badge bg-danger">High</span>
                            }
                            else if (task.Priority == "Medium")
                            {
                                <span class="badge bg-warning text-dark">Medium</span>
                            }
                            else if (task.Priority == "Low")
                            {
                                <span class="badge bg-success">Low</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">None</span>
                            }
                        </td>
                        <td>@(task.Deadline?.ToString("yyyy-MM-dd") ?? "No Deadline")</td>
                        <td>
                            <a asp-action="UpdateTask" asp-route-id="@task.Id" class="btn btn-sm btn-warning">Edit</a>
                        </td>
                    </tr>
                    i++;
                }
            }
        </tbody>
    </table>
    <!-- Back Button -->
    <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Back</a>
}
else
{
    <h4>No tasks assigned yet.</h4>
}
